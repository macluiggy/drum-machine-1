import React from "react"
import ReactDOM from "react-dom"
import "./index.css"
import App from "../src/components/App/App"
//hola, solo estoy aprendiendo a usar git para contribuir, no tienes que fisonar estos cambios xdd
//contenido que va a ser derivado en una rama local pero con un nombre diferente
ReactDOM.render(<App />, document.getElementById("root"))
